# Fully local stack (Ollama + Postgres) — NO API key required.
# This file is kept only as a reference in case you later switch to a paid
# provider (e.g. moving generation to Claude/OpenAI).
#
# ANTHROPIC_API_KEY=
